[package]
name = "ah-updater-core"
version = "1.1.0"
edition = "2024"
publish = false

[dependencies]
ah-release-manifest = { path = "../ah-release-manifest" }
semver = "1"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
base64 = "0.22"
ed25519-dalek = "3"
serde_json = "1"
