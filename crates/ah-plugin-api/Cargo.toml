[package]
name = "ah-plugin-api"
version = "0.1.0"
edition = "2024"
description = "Stable plugin contract and C ABI types for AIHelper"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
