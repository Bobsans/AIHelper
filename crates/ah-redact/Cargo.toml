[package]
name = "ah-redact"
version = "2.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Secret redaction and credential detection shared by every AIHelper sink"
# Distributed as signed binaries, not on crates.io. Publishing would also
# require versioned path dependencies across the workspace.
publish = false

[dependencies]
serde_json.workspace = true
shell-words.workspace = true

[dev-dependencies]
proptest.workspace = true
